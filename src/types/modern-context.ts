/**
 * Placeholder modern context container. Phase 1+ populates these contracts.
 */
export interface ModernContext {
  storyCards?: StoryCard[]
  entityGraph?: EntityGraph
  vizHints?: VizHint[]
  pivotTargets?: PivotTarget[]
}

/** Placeholder for Phase 3 story card typing. */
export interface StoryCard {
  /** TODO Phase 3 */
  _placeholder?: never
}

/** Placeholder for Phase 5 entity graph typing. */
export interface EntityGraph {
  /** TODO Phase 5 */
  _placeholder?: never
}

/** Placeholder for Phase 5 visualization hint typing. */
export interface VizHint {
  /** TODO Phase 5 */
  _placeholder?: never
}

/** Placeholder for Phase 2 pivot target typing. */
export interface PivotTarget {
  /** TODO Phase 2 */
  _placeholder?: never
}
